# Environment Configuration Template
# Info-Display-Wetter_Berlin_Adlershof

# Optional Weather Proxy Base URL (Cloudflare Worker Proxy)
WEATHER_PROXY_URL=https://weather-proxy.example.workers.dev

# Optional Provider API Keys (Used only if self-hosting the proxy)
OPENWEATHER_API_KEY=your_openweather_api_key_here
